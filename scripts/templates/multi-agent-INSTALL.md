# Unreal AngelScript Skill — Multi-Agent Bundle

This bundle pre-fans out the skill into every supported AI agent layout.
Pick the directory that matches the agent you use and copy it into
your project root (or your user-level config directory).

| Agent          | Path inside this bundle                  |
|----------------|------------------------------------------|
| Generic / spec | `skills/unreal-angelscript/`             |
| CodeBuddy      | `.codebuddy/skills/unreal-angelscript/`  |
| Claude Code    | `.claude/skills/unreal-angelscript/`     |
| Cursor         | `.cursor/skills/unreal-angelscript/`     |
| Windsurf       | `.windsurf/skills/unreal-angelscript/`   |
| Cline          | `.cline/skills/unreal-angelscript/`      |
| Roo Code       | `.roo/skills/unreal-angelscript/`        |
| Trae           | `.trae/skills/unreal-angelscript/`       |
| Kiro           | `.kiro/skills/unreal-angelscript/`       |
| OpenCode       | `.opencode/skills/unreal-angelscript/`   |
| AGENTS.md spec | `.agents/skills/unreal-angelscript/`     |

For automated installation, use `install.sh` or `install.ps1` from the
repository.
